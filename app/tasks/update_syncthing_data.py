# To get latest data from syncthing instead of using syncthing itself, we use rsync scheduled to run every 15 min.
# Syncthing itself takes lot of cpu and there is not really very frequent updation of school data, thus rsync is good enough for
# the task at hand.
